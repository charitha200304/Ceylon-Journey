# App name
spring.application.name=Travel_Agency

# MySQL DB Config
spring.datasource.url=jdbc:mysql://localhost:3306/Ceylon_Journey?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Ns20050727
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=true

# Spring Security default user (optional if using JWT)
spring.security.user.name=user1
spring.security.user.password=1234

# JWT Secret Key (Base64-encoded)
# You must generate a strong secret and encode it in Base64.
# Here's an example using 256-bit key: "my-very-secret-key" (base64 encoded)
jwt.secret=mypasswordisverysecretandlongandsecure